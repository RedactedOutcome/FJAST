#include "pch.h"
#include "ASTGenerator.h"

namespace FJASTP{
    ASTGeneratorResult ASTGenerator::Generate(const std::vector<Token>& input, std::vector<Node>& output){
        return ASTGeneratorResult();
    }
}