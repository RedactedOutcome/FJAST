

/// 5 * 2 * 2
/// (5 * 2) * 2
/// 5 * 2 * 2
/// (5 * 2) * 2
class HelloTest1{
    print(args1, Update){
        //Bug with (5 + 2) * 2
        jello(hello * 2);
        condsole.aySometjhing(HelloTes, AudioWorkletNode, 2 * /* */ ((2 + 4) + 8 * 8), GetCap(hello, hell2o, (5 + 2) * 2))
        /// TODO: have support for identifier++ * x
        HelloTest1(args1, 1)
        HelloTest1(Update++ * 2, 1);

        var var1 = 15;
        let var2 = 15;
        const constVariaable = 15;
        x *= 25;

        console.log = function hello(variableName){
            print("HEllo World");
            var test1 = var1 * 15;

            functionCall(teste1);
        }
        /*
        for(var i = 0; i < 15; i++){
            console.log("Hello World")
        }
        */

        x = functionCall(identifier * 15);
    }
}

/// 5 * 2 * 2
/// (5 * 2) * 2